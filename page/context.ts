export const hasTouch = /Android|iPhone|iPad|iPod/.test(navigator.userAgent)
export const isApple = navigator.userAgent.includes('Mac OS X')
export const isFirefox = navigator.userAgent.includes('Firefox')
